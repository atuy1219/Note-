name: Finalize lasso and Trash behavior

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize
      - reopened

permissions:
  contents: write

jobs:
  finalize-and-verify:
    if: github.head_ref == 'agent/toolbar-lasso-library-management'
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/toolbar-lasso-library-management
          fetch-depth: 0

      - name: Apply final behavior refinements
        run: python .github/finalize_lasso_trash_behavior.py

      - name: Set up JDK 17
        uses: actions/setup-java@v5.7.0
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android SDK 37
        run: sdkmanager 'platforms;android-37.0' 'build-tools;37.0.0'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6.2.0
        with:
          gradle-version: '9.5.0'

      - name: Build and test
        shell: bash
        run: |
          set -euo pipefail
          gradle :app:assembleDebug :app:assembleRelease :app:testDebugUnitTest --stacktrace

      - name: Persist final behavior refinements
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            app/src/main/java/com/atuy/note/MainViewModel.kt \
            app/src/main/java/com/atuy/note/ink/InkPageView.kt
          git rm .github/finalize_lasso_trash_behavior.py
          git rm .github/workflows/finalize-lasso-trash.yml
          git diff --cached --check
          git commit -m 'Finalize lasso drag threshold and Trash root items'
          git push origin HEAD:agent/toolbar-lasso-library-management
